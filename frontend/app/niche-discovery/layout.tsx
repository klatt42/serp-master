export const metadata = {
  title: 'Niche Discovery - SERP-Master',
  description: 'Find profitable keyword opportunities and content gaps',
};

export default function NicheDiscoveryLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
